# Use flat node_modules (like npm) - fixes use-sync-external-store CJS/ESM
# interop with Vite. Working money-saver used npm; pnpm's default structure
# causes Vite to serve raw CJS and fail.
node-linker=hoisted
